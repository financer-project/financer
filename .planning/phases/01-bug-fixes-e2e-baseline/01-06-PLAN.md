---
phase: 01-bug-fixes-e2e-baseline
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md
  - test/vitest/**/*.test.ts
autonomous: true
gap_closure: true
requirements: [TEST-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "LCOV coverage after the sharded-matrix speed-up is equal to or higher than the pre-change baseline (ROADMAP Phase 1 Success Criterion 4; REQUIREMENTS.md TEST-01: 'coverage no worse than the current baseline') (01-VERIFICATION.md failed truth #14, restated verbatim — this plan is the closure attempt)"
    - "The shortfall is named, not merely described: every source line that was hit in the Before merged LCOV and unhit in the After samples is enumerated by file path and line number in BASELINE.md"
    - "Each enumerated dropped line is either newly covered by a test that asserts the real behaviour of that line, or carries an individual written reason it cannot be deterministically covered"
    - "LF (lines found) is unchanged by the closure work, so the After/Before comparison stays like-for-like on the same instrumented line set (3827 in every sample recorded to date)"
    - "Any added test asserts observable behaviour and would fail if that behaviour changed — coverage is a by-product of the assertion, never its purpose"
    - "The coverage measurement machinery is untouched: nyc.config.js, vitest.config.ts, cypress.config.ts and the coverage-merge script are byte-identical before and after this plan"
    - "The edge boundary for TEST-01's coverage criterion is treated strictly: 'equal to' passes and 'one line lower' fails; no rounded percentage is used to decide it (edge-probe TEST-01/unclassified, coverage-parity boundary)"
  prohibitions:
    - statement: "MUST NOT close the coverage gap by suppressing measurement — no coverage-ignore pragmas in source, no new exclude entries or threshold changes in nyc.config.js or vitest.config.ts, and no source file removed from the instrumented set"
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT add a test whose only effect is to execute a line — every added test must assert the behaviour of the code it covers and must be able to fail if that behaviour regresses"
      category: values
      status: resolved
      verification: judgment
    - statement: "MUST NOT change LF (the instrumented line set) as a way of moving the LH/LF ratio, and MUST NOT compare Before against After on a rounded percentage when the raw totals disagree"
      category: transparency
      status: resolved
      verification: test
  artifacts:
    - path: ".planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md"
      provides: "Enumerated per-file, per-line coverage delta between the Before baseline and the post-sharding samples, with a per-line classification and the closure taken"
      contains: "Coverage gap-closure investigation"
  key_links:
    - from: ".planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md"
      to: "GitHub Actions merged-lcov artifacts"
      via: "every enumerated line is traceable to the named run its Before and After hit counts were read from"
      pattern: "https://github.com/"
    - from: "test/vitest"
      to: "src"
      via: "new or extended unit tests assert behaviour of the specific source lines enumerated as dropped"
      pattern: "describe\\("
---

<objective>
Close 01-VERIFICATION.md gap 2 at its root: find out exactly which source lines stopped being covered when the E2E suite moved from one sequential job to four parallel shards, and cover them deterministically so the merged LH/LF is no longer below the pre-change baseline.

Purpose: BASELINE.md diagnoses the 7-10 line shortfall as "most plausibly execution-order-sensitive" but never identifies a single line. A diagnosis without line numbers cannot be acted on and cannot be defended. This plan converts the hand-wave into a list, then closes the list — preferring a deterministic unit test over an order-dependent E2E side effect, which is exactly the lever PITFALLS #8 recommends and is strictly better than what the baseline had.
Output: An enumerated per-line delta recorded in BASELINE.md, and new or extended tests that raise merged LH back to at least the Before figure without touching any coverage configuration.

**MVP-mode note:** the ROADMAP `**Goal:**` line for this phase is an outcome sentence, not `As a / I want to / so that` user-story form; no story was invented here. Task 1 is the tracer for this plan — it drives the thinnest complete path (download both merged LCOV artifacts → parse → produce a concrete named list of dropped lines) and settles up front whether the gap is closable at all, before any test-writing effort is spent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes-e2e-baseline/01-CONTEXT.md
@.planning/phases/01-bug-fixes-e2e-baseline/01-VERIFICATION.md
@.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md
@.planning/phases/01-bug-fixes-e2e-baseline/deferred-items.md
@.planning/research/PITFALLS.md
</context>

<interface_context>
Facts the executor needs without re-deriving them:

- **The exact numbers in dispute.** Before: LH `2956` / LF `3827` = 77.24%, from run `32869899958`. After sample 1: LH `2949` / LF `3827` = 77.06%, from run `32887712546`. After sample 2: LH `2946` / LF `3827` = 76.98%, from run `32905249686`. LF matches exactly in all three, so the instrumented line set is identical and the entire difference is 7-10 lines that flipped from hit to unhit.
- **Where the artifacts are.** `.github/workflows/ci.yml`'s `merge-coverage-and-sonar` job uploads the merged file as the artifact named `merged-lcov` (path `.test/lcov.info`). The Before run `32869899958` predates that step and used an ad-hoc upload added on a temporary branch, so its artifact name may differ — list it with `gh api repos/{owner}/{repo}/actions/runs/32869899958/artifacts` rather than guessing. BASELINE.md's Provenance section links every run under `github.com/financer-project/financer`, while the local `origin` remote is `github.com/raphaelmue/financer-blitz`; resolve which repository actually owns these run IDs before downloading and record the answer.
- **LCOV parsing.** The merged file has no `LF:`/`LH:` summary records — `lcov-result-merger` emits only the raw per-line `DA:<line>,<hits>` records under each `SF:<path>` header. LF is the count of `DA:` records; LH is the count with a hit count greater than zero. A "dropped" line is one whose `(SF, line)` pair has hits greater than zero in Before and exactly zero in an After sample.
- **Line numbers are comparable.** The Before baseline was captured at commit `db3ef6c377194d2f4e602d194c114480c78f044e`, which already contained plans `01-01` and `01-02` (BASELINE.md's "Ordering rationale"). Everything since has been CI and config work, not `src/` edits — so `src/` line numbers should align. Confirm rather than assume: if any `SF:` section has a different `DA:` record count between Before and After, that file's lines moved and it must be handled separately.
- **The instrumented set is fixed, not import-driven.** `nyc.config.js` sets `all: true` with `include: ["src/**/*.ts", "src/**/*.tsx"]`, and `vitest.config.ts` sets `coverage.include: ["src/**"]`. Both therefore report every matching source file whether or not a test imports it. Adding a test that imports a previously-untested file should raise LH without changing LF — but verify this empirically rather than trusting it, because a changed LF would invalidate the like-for-like comparison.
- **Known, already-diagnosed non-cause.** Component-test coverage has never been collected at all (`test/cypress/support/component.tsx` does not import `@cypress/code-coverage/support`), identically in Before and After. This is recorded in `deferred-items.md` and is explicitly out of scope — fixing it would add lines to After with no matching Before figure and would break the comparison.
- **Local coverage commands.** `yarn test:unit` runs Vitest with coverage into `.test/unit/coverage/`. `yarn coverage:merge` runs `lcov-result-merger ".test/**/*.info" .test/lcov.info`. Unit tests live under `test/vitest/`, mirroring `src/`'s directory shape, with Prisma mocked via `test/vitest/setup/mock-prisma.ts`.
- **File ownership this wave.** Plan `01-05` runs in the same wave and owns `test/cypress/e2e/transactions.spec.ts`, `test/cypress/support/e2e.ts` and `.github/workflows/ci.yml`. This plan must not edit any of those three files.
</interface_context>

<tasks>

<task type="tracer">
  <name>Task 1: Enumerate the exact source lines that stopped being covered, and record them in BASELINE.md</name>
  <precondition>`gh auth status` succeeds, and `gh api repos/{owner}/{repo}/actions/runs/32869899958/artifacts` returns at least one artifact entry — i.e. the Before merged-LCOV artifact has not passed its retention window. If it has expired, do not fabricate a delta: record the expiry in the SUMMARY and switch to the fallback path described in the action.</precondition>
  <files>.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</files>
  <read_first>
    - .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md (the "Coverage comparison (D-10, criterion 4)" table, the "Verdict" section's criterion 4 paragraph, and the "Provenance" section listing every run ID this plan needs)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-VERIFICATION.md (gap 2 — the failed truth this plan closes, and the wording a future verifier will match against)
    - .planning/phases/01-bug-fixes-e2e-baseline/deferred-items.md (the component-coverage gap that is explicitly NOT the cause and must stay out of scope)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-CONTEXT.md (D-10, D-11 — committed artifact, every figure sourced from a real CI run)
    - nyc.config.js and vitest.config.ts (the instrumented set definition — read to confirm LF is not import-driven, and to confirm neither file may be edited)
  </read_first>
  <behavior>
    - Three merged LCOV files are obtained: one Before and two After, each from its named run.
    - The `SF:` file list and per-file `DA:` record counts are compared across all three; any mismatch is reported rather than silently reconciled.
    - The set of `(file, line)` pairs hit in Before and unhit in After sample 1 is computed, and the same for After sample 2.
    - The intersection (lines dropped in both samples) and the union (lines dropped in either) are both reported, since the baseline's own 7-vs-10 spread implies some lines are unstable across runs.
    - Each dropped line is listed with its source text and the enclosing function or block.
  </behavior>
  <action>
Resolve which GitHub repository owns the run IDs recorded in BASELINE.md's Provenance section, then download the merged LCOV artifact from each of run `32869899958` (Before), run `32887712546` (After sample 1) and run `32905249686` (After sample 2) with `gh run download`. Discover each artifact's actual name first via `gh api repos/{owner}/{repo}/actions/runs/{id}/artifacts` — the Before run predates the permanent `merged-lcov` upload step and may carry a different name. Keep the three downloaded files in a scratch location outside the repository working tree, or delete them before committing; they must not end up in the commit.

Parse each file with a throwaway Node script. Group `DA:<line>,<hits>` records under their preceding `SF:<path>` header. First run the integrity checks: assert the `SF:` path sets are identical across all three files, assert the total `DA:` record count is `3827` in each, and assert each individual file's `DA:` record count matches across all three. Report any mismatch loudly and stop — a changed record count means source line numbers moved between the Before commit and the After commits, and every subsequent comparison would be meaningless. Then confirm the LH totals reproduce the figures already in BASELINE.md (`2956`, `2949`, `2946`); if they do not, the wrong artifacts were downloaded.

Compute the drop sets: pairs whose Before hit count is greater than zero and whose After hit count is exactly zero, once per After sample. Report the intersection (dropped in both samples — the stable core of the shortfall) and the union (dropped in at least one — including the run-to-run unstable lines that explain the 7-versus-10 spread). Also compute the reverse direction — lines unhit in Before but hit in After — because a net figure of 7 could be composed of a larger number of drops partly offset by gains, and that changes what has to be covered.

For every line in the union, read the enclosing source file and record the line's actual source text plus the function, branch or block it sits in. Classify each one into exactly one bucket: pure logic reachable from a unit test; behaviour only reachable through a browser interaction; an error or catch path only reachable under a fault the test suite does not induce; or genuinely dead or unreachable code. The classification drives Task 2 — it is not decoration.

Add a new section to `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md` titled "Coverage gap-closure investigation (plan 01-06)", placed after the existing "Coverage comparison (D-10, criterion 4)" subsection. Record: the method and the exact commands used; the integrity-check results; the drop table with one row per line (file, line number, source text, classification, dropped-in-both-or-one); the reverse-direction gains if any; and the resulting count reconciled against the 7-10 figure BASELINE.md already reports. Attribute every figure to the run ID it came from, per D-11, and extend the Provenance section if any new run is referenced. Do not edit or soften the existing "Verdict" section in this task — the verdict is rewritten in plan `01-07` once the closure outcome and a fresh CI number exist.

Fallback when the Before artifact has expired: do not invent a delta and do not compare against a locally recomputed substitute. Record the expiry plainly in BASELINE.md and the SUMMARY, and note that the closure path degrades to the second-best option — raising merged LH above `2956` by adding deterministic coverage to genuinely under-tested logic, which satisfies the numeric bar without explaining the original drop. Task 2 handles both paths.

Do not touch `nyc.config.js`, `vitest.config.ts`, `cypress.config.ts`, `package.json`'s coverage scripts, or any file owned by plan `01-05` (`test/cypress/e2e/transactions.spec.ts`, `test/cypress/support/e2e.ts`, `.github/workflows/ci.yml`).
  </action>
  <verify>
    <automated>grep -c 'Coverage gap-closure investigation' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
    <automated>grep -c 'https://github.com/' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
    <automated>git diff --exit-code -- nyc.config.js vitest.config.ts cypress.config.ts</automated>
    <automated>git status --porcelain | grep -c 'lcov.info' || true</automated>
    <automated>git diff --name-only HEAD | sort</automated>
  </verify>
  <acceptance_criteria>
    - BASELINE.md contains a section titled "Coverage gap-closure investigation (plan 01-06)".
    - That section contains a table with one row per dropped line, each row carrying a source file path, a line number, the line's source text, and a classification from the four buckets named in the action.
    - The number of rows reconciles with the 7-10 line figure already recorded in BASELINE.md, and any discrepancy is explained in prose.
    - The section states the integrity-check outcomes explicitly: identical `SF:` path sets across all three files, per-file `DA:` record counts matching, and total `DA:` records equal to 3827 in each.
    - The recomputed LH totals stated in the section equal 2956, 2949 and 2946 respectively, matching the figures already in BASELINE.md.
    - Every figure in the new section names the GitHub Actions run ID it was read from.
    - `git diff --exit-code -- nyc.config.js vitest.config.ts cypress.config.ts` produces no output.
    - `git diff --name-only HEAD` for this task lists only `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md` — no downloaded artifact, scratch script, or coverage output is committed.
    - If the Before artifact had expired, the section says so explicitly and states that the fallback path is in use, rather than presenting an unsourced delta.
  </acceptance_criteria>
  <done>The coverage shortfall is a named list of source lines with classifications, not a plausible-sounding explanation — so Task 2 has something concrete to close.</done>
</task>

<task type="auto">
  <name>Task 2: Cover the enumerated lines deterministically, or classify each one as uncloseable with a reason</name>
  <files>test/vitest/**/*.test.ts, .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</files>
  <read_first>
    - The drop table written by Task 1 into BASELINE.md (the specific lines to close and their classifications)
    - Each source file named in that table (the enclosing function, its inputs, and what a real assertion on it would look like)
    - test/vitest/lib/util/prisma-filter-builder.test.ts and test/vitest/lib/model/transactions/transaction.test.ts (the house style for unit tests: describe/it structure, Prisma mocking, assertion style)
    - test/vitest/setup/mock-prisma.ts (how the Prisma client is mocked for unit tests)
    - vitest.config.ts (test directory, coverage include/exclude, reports directory — read to know where output lands, not to edit)
    - .planning/research/PITFALLS.md (Pitfall 8 — moving unit-testable logic into Vitest is the sanctioned lever; weakening retries or cleanup is not)
  </read_first>
  <behavior>
    - Every line classified as pure logic in Task 1 is exercised by a new or extended unit test that asserts what the line actually does.
    - Each added test fails if the behaviour it asserts is changed — verified by temporarily breaking the asserted behaviour and observing the test go red.
    - Local unit-run LF is unchanged by the additions; only LH moves.
    - Lines that cannot be closed carry an individual written reason, not a blanket statement.
    - No coverage configuration, threshold, exclusion or source-level suppression is introduced.
  </behavior>
  <action>
Work the drop table one classification at a time.

For lines classified as pure logic reachable from a unit test: add or extend a Vitest test under `test/vitest/`, mirroring the directory shape of the source file, following the describe/it and Prisma-mocking style of the existing tests named in `read_first`. Each test must assert the real behaviour of the code — the returned value, the thrown error type and message, the shape of the constructed query — and must not merely call the function for its side effect on the coverage counter. A test that would still pass if the line's logic were inverted is worthless here and is explicitly out of bounds.

For lines classified as browser-only behaviour: prefer a targeted assertion added to an existing Cypress E2E spec that already exercises the surrounding flow. Do not create a new spec file — the shard count is currently equal to four and the spec count is fifteen, and adding specs changes the distribution the recorded baseline was measured against. Never edit `test/cypress/e2e/transactions.spec.ts` or `test/cypress/support/e2e.ts`: plan `01-05` owns both this wave. If the only sensible home for the assertion is one of those two files, stop, record it in the SUMMARY, and leave the line for the decision checkpoint in plan `01-07`.

For lines classified as fault-only error paths: attempt a unit test that induces the fault through the existing mock seam (a rejected Prisma call, a thrown parse error) rather than through timing. If the fault cannot be induced without contorting production code, leave the line uncovered and write down why.

For lines classified as dead or unreachable: do not delete the code as a way of raising the ratio — removing lines changes LF and breaks the like-for-like comparison, which is exactly what this plan must not do. Record the finding for a future cleanup effort instead.

On the fallback path (Before artifact expired, no drop table): choose genuinely under-tested logic in `src/lib/model/` or `src/lib/util/` with a real behavioural gap, and add tests for it until the local unit LH rises by more than the 10-line worst-case shortfall. State plainly in BASELINE.md that this path was taken and that it satisfies the numeric bar without explaining the original drop.

After the tests are written, measure locally. Record the `.test/unit/coverage/lcov.info` LF and LH totals before your additions and again after, computed the same way as Task 1 (count of `DA:` records, and count with hits greater than zero). LF must be identical across the two measurements — if it moved, a source file entered or left the instrumented set and the like-for-like comparison is compromised; diagnose that before proceeding. LH must have risen by at least the union-size of the drop table, or by more than 10 on the fallback path.

Then prove the new tests have teeth. For each new test file, temporarily invert or break the behaviour it asserts in the source, run `yarn test:unit` and confirm the test goes red, then revert. Commit only the reverted state and record the observation in the SUMMARY.

Append the outcome to BASELINE.md's "Coverage gap-closure investigation (plan 01-06)" section: which lines were closed and by which test, which were not and why, and the local before/after LF and LH figures with a note that the authoritative merged figure comes from the CI run in plan `01-07`. If any line remains uncovered, say so here plainly — plan `01-07` needs an accurate list to put in front of a human.

Do not add coverage-ignore pragmas to any source file. Do not add exclude entries or thresholds to `nyc.config.js` or `vitest.config.ts`. Do not modify `cypress.config.ts` or `package.json`'s coverage scripts. Do not delete or skip any existing test.
  </action>
  <verify>
    <automated>yarn test:unit</automated>
    <automated>yarn tsc --noEmit</automated>
    <automated>git diff --exit-code -- nyc.config.js vitest.config.ts cypress.config.ts package.json</automated>
    <automated>grep -rc 'istanbul ignore' src/ 2>/dev/null | grep -v ':0$' | wc -l</automated>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.test/unit/coverage/lcov.info','utf8').split(/\r?\n/).filter(l=>l.startsWith('DA:'));console.log('LF='+t.length+' LH='+t.filter(l=>Number(l.split(',')[1])>0).length)"</automated>
    <automated>git diff --name-only HEAD | sort</automated>
  </verify>
  <acceptance_criteria>
    - `yarn test:unit` exits 0 and `yarn tsc --noEmit` exits 0.
    - `grep -rc 'istanbul ignore' src/ | grep -v ':0$' | wc -l` returns 0 — no source file carries a coverage-suppression pragma.
    - `git diff --exit-code -- nyc.config.js vitest.config.ts cypress.config.ts package.json` produces no output.
    - The locally computed unit-coverage LF is identical before and after the additions; the figure is recorded in BASELINE.md.
    - The locally computed unit-coverage LH rose by at least the union-size of Task 1's drop table (or by more than 10 on the fallback path); the figures are recorded in BASELINE.md.
    - Every line in Task 1's drop table appears in the outcome table with either the test that now covers it or an individual written reason it could not be covered — no line is silently omitted.
    - The SUMMARY records, for each new test file, the deliberate source break that turned it red and confirms it was reverted.
    - `git diff --name-only HEAD` for this task lists only files under `test/vitest/` and `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md`.
    - No file owned by plan `01-05` (`test/cypress/e2e/transactions.spec.ts`, `test/cypress/support/e2e.ts`, `.github/workflows/ci.yml`) appears in this task's diff.
  </acceptance_criteria>
  <done>The named coverage shortfall is closed by tests that assert real behaviour, or every remaining line carries a reason a human can weigh — with the measurement machinery provably untouched either way.</done>
</task>

</tasks>

<planner_assumptions>
Explicit, surfaced assumptions and probe dispositions for this plan — none silently dropped:

1. **Edge-probe `TEST-01 / unclassified` — RESOLVED (explicit) for the coverage-parity boundary.** Manual review identifies the boundary as the `>=` comparison itself: "equal to" is inside the bar and "one line lower" is outside. The plan treats it strictly and on raw LF/LH totals, never a rounded percentage, because 77.06% and 77.24% could both round to the same figure at lower precision. The shard-count boundary — TEST-01's other edge — is owned by plan `01-05` Task 3, not this plan.
2. **Prohibition-probe canon referrals — breadcrumbed, not minted.** No user-facing surface, no data handling, no auth path is touched here. Stage 1 candidates around CI secret handling are canon owned by `/gsd-secure-phase` and are breadcrumbed rather than minted. Stage 2 dropped the routine-engineering candidates (test isolation, mock hygiene). What remains and is genuinely bespoke is the measurement-integrity family: do not suppress measurement, do not write assertion-free tests, do not move LF. Those three are minted in `must_haves.prohibitions`.
3. **Prohibition-probe: the layered-architecture constraint from `.claude/CLAUDE.md` is DROPPED at Stage 2** as routine engineering rather than an intent constraint, and no `src/` production code is modified by this plan in any case.
4. **Line-number comparability is checked, not assumed.** The Before commit already contained `01-01` and `01-02`, and everything since has been CI/config work — but Task 1 asserts identical per-file `DA:` record counts across all three artifacts before comparing, rather than trusting that reasoning.
5. **Artifact retention is a real risk with a documented fallback.** GitHub Actions artifacts expire. If the Before merged LCOV is gone, the forensic path is unavailable and the plan degrades to raising LH above the recorded `2956` by adding deterministic coverage — numerically sufficient, forensically weaker, and labelled as such in BASELINE.md rather than presented as equivalent.
6. **Component coverage stays out of scope.** `deferred-items.md` records that component-test coverage has never been collected, identically in Before and After. Fixing it would inflate After against a Before that never included it. It is not a cause of this gap and must not be used as a lever to close it.
7. **A partial closure is a valid outcome of this plan.** If some lines genuinely cannot be deterministically covered, this plan completes by naming them accurately. Deciding what to do about a residual shortfall is plan `01-07`'s checkpoint, not something this plan may settle by adjusting a number.
8. **UI-SPEC lift is not applicable.** `01-UI-SPEC.md` records that TEST-01 has no UI surface; every non-dismissed row was lifted into plans `01-01` and `01-02`. Nothing is dropped by this plan's silence.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI artifact → committed project documentation | Coverage figures cross from an external system into a durable artifact the phase verifier relies on as evidence for a roadmap success criterion |
| Test suite → coverage measurement | The number that decides a requirement is produced by the same suite the change is allowed to modify, so the measurement machinery must be provably untouched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Tampering | Coverage configuration (`nyc.config.js`, `vitest.config.ts`, `cypress.config.ts`, `package.json` scripts) | high | mitigate | The cheapest way to "close" a coverage gap is to shrink what is measured. All four files are asserted byte-identical by `git diff --exit-code` in both tasks' acceptance criteria, and source-level coverage-suppression pragmas are gated to zero. |
| T-01-18 | Tampering | Newly added tests | medium | mitigate | A test that executes a line without asserting it raises the number while proving nothing. Every new test file must be observed going red against a deliberate break in the source it asserts, and that observation must be recorded in the SUMMARY. |
| T-01-19 | Repudiation | The enumerated delta in BASELINE.md | medium | mitigate | Every figure must name the run ID it was read from, and the recomputed LH totals must reproduce the figures already recorded (2956 / 2949 / 2946). A delta that cannot be reproduced from a named artifact is indistinguishable from a guess. |
| T-01-20 | Information Disclosure | Downloaded coverage artifacts in the working tree | low | mitigate | Merged LCOV files carry source paths and line data, not secrets, but they are large and must not be committed. Both tasks gate on `git diff --name-only HEAD` listing only the intended files. |

No package-manager install occurs in this plan, so no `T-01-SC` supply-chain row applies. No schema-relevant file (Prisma schema, migration) is touched, so no schema-push task is present. No new attack surface is introduced — this is test and documentation work against existing code.
</threat_model>

<verification>
- BASELINE.md contains a "Coverage gap-closure investigation (plan 01-06)" section whose drop table names every affected file and line number, each with a classification.
- The recomputed LH totals in that section equal 2956 (Before), 2949 (After sample 1) and 2946 (After sample 2).
- `yarn test:unit` exits 0 and `yarn tsc --noEmit` exits 0.
- `git diff --exit-code -- nyc.config.js vitest.config.ts cypress.config.ts package.json` produces no output.
- `grep -rc 'istanbul ignore' src/ | grep -v ':0$' | wc -l` returns 0.
- Locally computed unit-coverage LF is unchanged across the plan; LH rose by at least the drop table's union size.
- No file owned by plan `01-05` appears in this plan's diff.
- Every line in the drop table has either a covering test or an individual written reason in the outcome table.
</verification>

<success_criteria>
The 7-10 line coverage shortfall behind 01-VERIFICATION.md's failed truth #14 stops being an unexplained rounding-scale gap and becomes a named list of source lines, each either newly covered by a test that asserts real behaviour or accompanied by a specific reason it cannot be. The measurement machinery is provably unchanged, LF is provably unchanged, and any residual shortfall is stated accurately enough for a human to decide on it in plan `01-07`.
</success_criteria>

<artifacts_this_phase_produces>
Symbols this plan creates or modifies (new/changed — exclude from drift verification against pre-existing code):

- New section "Coverage gap-closure investigation (plan 01-06)" in `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md`, containing the method, integrity-check results, the per-line drop table, the reverse-direction gains, the closure outcome table, and the local before/after LF and LH figures.
- Extended "Provenance" entries in the same file for any run ID newly referenced.
- New or extended Vitest test files under `test/vitest/`, mirroring the directory shape of the source files named in the drop table. Exact paths are determined by Task 1's diagnosis and must be listed in the SUMMARY.
- New `describe` / `it` blocks inside those files asserting the behaviour of the specific source lines enumerated as dropped.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-bug-fixes-e2e-baseline/01-06-SUMMARY.md` when done.
</output>
