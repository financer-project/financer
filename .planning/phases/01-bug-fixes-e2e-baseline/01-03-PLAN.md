---
phase: 01-bug-fixes-e2e-baseline
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md
  - package.json
  - yarn.lock
  - cypress.config.ts
  - .github/workflows/ci.yml
autonomous: false
requirements: [TEST-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "BASELINE.md exists in the phase directory and records pre-change CI wall-clock time and merged LCOV coverage read from a named, linked GitHub Actions run (D-10, D-11)"
    - "The pre-change baseline is measured on the same spec set that the sharded suite will run, so the before/after comparison is like-for-like"
    - "The E2E job runs as a GitHub Actions matrix; each shard job runs a distinct subset of the 14 specs on its own runner with its own Testcontainers MySQL and Redis instance (D-07, D-08)"
    - "Each E2E shard uploads its own coverage artifact, and a single downstream job merges every shard's artifact plus the unit and component artifacts into .test/lcov.info before SonarQube runs exactly once (D-07)"
    - "retries.runMode remains 2 and all after()/after:run cleanup remains in place after the restructure"
    - "All 14 E2E spec files still execute across the shards — no spec is deleted, disabled or excluded"
    - "Every E2E shard receives at least one spec, so no shard fails on an empty spec set"
  prohibitions:
    - statement: "MUST NOT obtain the speed-up by removing, disabling or weakening test coverage — no deleted or excluded spec files, no lowering of retries.runMode below 2, and no removal of after() or after:run cleanup"
      category: values
      status: resolved
      verification: judgment
    - statement: "MUST NOT record any number in BASELINE.md that was not read from an actual, linkable GitHub Actions run — no locally-timed, estimated, or recalled figures"
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md"
      provides: "Committed pre-change CI wall-clock and LCOV coverage baseline with source run links (D-10, D-11)"
      contains: "Before"
    - path: ".github/workflows/ci.yml"
      provides: "Multi-job CI graph with a sharded E2E matrix and a single downstream coverage-merge plus SonarQube job"
      contains: "matrix"
    - path: "cypress.config.ts"
      provides: "cypress-split spec distribution and disabled video recording for the e2e run"
      contains: "cypress-split"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "cypress.config.ts"
      via: "SPLIT and SPLIT_INDEX env vars sourced from strategy.job-total / strategy.job-index and consumed by cypress-split in setupNodeEvents"
      pattern: "SPLIT"
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "the merge job runs yarn coverage:merge (lcov-result-merger) after downloading every shard artifact"
      pattern: "coverage:merge"
    - from: ".github/workflows/ci.yml"
      to: "sonar-project.properties"
      via: "SonarQube reads sonar.javascript.lcov.reportPaths=.test/lcov.info, produced only by the merge job"
      pattern: "lcov.info"
---

<objective>
Record the pre-change CI baseline from a real CI run, then restructure CI so the Cypress E2E suite runs as a sharded GitHub Actions matrix with per-shard database isolation and a single downstream coverage merge.

Purpose: TEST-01 (#71). The E2E suite is the guard rail for the rest of this milestone — Phases 2 through 5 all depend on being able to rerun it cheaply. This plan captures the "before" number that phase success criteria 3 and 4 are measured against, then lands the architecture change.
Output: A committed `BASELINE.md` with the pre-change numbers, `cypress-split` wired into `cypress.config.ts`, and `.github/workflows/ci.yml` restructured into a build/unit/component/e2e-matrix/merge job graph.

**Ordering rationale (why this plan is wave 2, not wave 1):** plans `01-01` and `01-02` each add new E2E specs. Capturing the baseline after they land means the before-run and the after-run execute the same spec set, so the wall-clock and coverage comparison is like-for-like rather than confounded by test-count growth.

**MVP-mode note:** the ROADMAP `**Goal:**` line for this phase is an outcome sentence, not `As a / I want to / so that` user-story form; no story was invented here. Vertical-slice substance is honored — Task 3 is a tracer that drives a single real CI run all the way through the new job graph, from checkout to a merged `lcov.info` consumed by SonarQube, before any shard-count tuning happens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug-fixes-e2e-baseline/01-CONTEXT.md
@.planning/phases/01-bug-fixes-e2e-baseline/01-RESEARCH.md
@.planning/phases/01-bug-fixes-e2e-baseline/01-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/research/PITFALLS.md
</context>

<interface_context>
Facts verified this session — the executor should not re-derive these:

- **Spec count:** `test/cypress/e2e/` contains exactly 14 `*.spec.ts` files: accounts, adminSettings, attachments, authentication, category, counterparties, household, imports, mobile, onboarding, settings, tags, transactionTemplates, transactions. Plan `01-02` adds a 15th (`formKeyboardNavigation.spec.ts`), so re-count at execution time rather than assuming 14.
- **Coverage output directories:** unit → `.test/unit/coverage/` (`vitest.config.ts` `reportsDirectory`); component → `.test/component/coverage/` (`nyc --report-dir` in the `cypress:component` script); e2e → `.test/e2e/coverage/` (`nyc --report-dir` in the `cypress:e2e` script). Merged output → `.test/lcov.info`.
- **Merge command:** `yarn coverage:merge` is `lcov-result-merger ".test/**/*.info" .test/lcov.info`. `lcov-result-merger@5.0.1` is already a devDependency — no new package is needed for merging.
- **SonarQube input:** `sonar-project.properties` sets `sonar.javascript.lcov.reportPaths=.test/lcov.info`. Sonar must therefore run only after the merge, and exactly once.
- **Current workflow:** `.github/workflows/ci.yml` has one job with `id: ci` and `name: Build, Check & Test`, running checkout → corepack → setup-node → cache → install → build → lint → unit → component → e2e → merge → Sonar (PR variant and main variant) → upload screenshots on failure.
- **cypress.config.ts:** `retries: { runMode: 2, openMode: 0 }` at the top level. The `e2e` block registers `codeCoverageTask(on, config)`, `TestUtilityDBContainer.getInstance()`, four `on("task", ...)` handlers, `on("before:run")` → `startDatabase()`, `on("after:run")` → `stopDatabase()`, then `return config`. `video` is not set, so it defaults to `true`.
- **Testcontainers ports:** `test/utility/TestUtilityDBContainer.ts` binds MySQL host port 3307 and Redis host port 6380, hardcoded. These only collide if two Cypress processes share one machine — a GitHub Actions matrix gives each shard its own VM, so **no source change to that file is needed** (D-08 is satisfied by architecture).
- **Build output:** `yarn build` is `dotenv -e .env.production yarn db:generate && blitz build`, producing `.next/`. `yarn db:generate` (`prisma generate`) writes into `node_modules`, so it is cheap to re-run per job once dependencies are installed.
- **App start for E2E:** `yarn test:start:app` is `dotenv -e .env.test blitz start`, which requires a built `.next/`.
- **cypress-split contract:** it reads the `SPLIT` and `SPLIT_INDEX` environment variables and narrows the current process's spec list accordingly. It is a companion to a GitHub Actions matrix, not an alternative to one — it does not run multiple workers in a single process.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Read the pre-change CI baseline off a real GitHub Actions run and commit it</name>
  <precondition>The `gh` CLI is installed and authenticated against the `raphaelmue/financer-blitz` repository (`gh auth status` succeeds), and the current branch pushes trigger the CI workflow — `.github/workflows/ci.yml` fires on `pull_request` for `'**'` branches.</precondition>
  <reversibility rating="reversible">A new documentation artifact in the phase directory; deleting it restores the prior state.</reversibility>
  <files>.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md, .github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the current single-job workflow whose duration is the baseline)
    - package.json (the `test`, `cypress:e2e` and `coverage:merge` scripts that define what the E2E step actually runs)
    - sonar-project.properties (which lcov path is consumed)
    - nyc.config.js and vitest.config.ts (where each coverage report is written)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-CONTEXT.md (D-10 and D-11 — the artifact and provenance requirements)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-PATTERNS.md ("No Analog Found" — the recommended BASELINE.md structure, since no prior baseline doc exists in this repo)
  </read_first>
  <action>
Trigger a CI run on the current branch with the workflow still in its unmodified, single-job sequential form, so the recorded numbers describe the pre-change pipeline. Push the branch (the plans from `01-01` and `01-02` are already committed on it, so the spec set now matches what the sharded suite will run) and let the existing workflow execute.

Locate the run with `gh run list --branch <branch> --workflow "CI Workflow" --limit 5` and read its details with `gh run view <run-id>`. Record, from that run and no other source: the total workflow wall-clock duration, the duration of the "Run E2E Tests" step specifically, the run id, and the run's HTML URL. Use `gh run view <run-id> --log` or the job timing view to isolate the E2E step duration — the whole-workflow number alone is not enough, because the later comparison needs to attribute the improvement to the E2E stage.

Obtain the merged coverage from that same run rather than recomputing it locally. If the workflow does not already publish `.test/lcov.info` as an artifact, add an `actions/upload-artifact` step for it as part of this task and re-run so the baseline coverage is downloadable and attributable; the alternative — reading a coverage percentage off SonarQube — is acceptable only if the SonarQube analysis is linked to the same run id. Download the artifact with `gh run download <run-id>` and compute the line-coverage percentage from the LCOV `LF`/`LH` totals. Record the raw `LF` and `LH` totals alongside the percentage so the later comparison is exact rather than rounded.

Also record the inputs that make the comparison reproducible: the number of `*.spec.ts` files under `test/cypress/e2e/` at the time of the run (count them, do not assume), the runner label, and the commit SHA the run was triggered from.

Write all of this into a new `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md`. Structure it with a short purpose paragraph, a "Before (sequential, single job)" section holding the numbers above, an empty "After (sharded matrix)" section for plan `01-04` to fill, an empty "Verdict" section, and a "Provenance" section listing every run id and URL the numbers came from. State explicitly in the file that every figure was read from a named CI run, per D-11.

Do not estimate, extrapolate, or fill any figure from a local timing measurement — a dev machine is not representative of a CI runner, which is exactly why D-11 requires CI-sourced numbers.
  </action>
  <verify>
    <automated>test -s .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
    <automated>grep -c 'https://github.com/' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
    <automated>grep -Ec 'LF|LH' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md` exists and is non-empty.
    - It contains a "Before" section with: total workflow duration, E2E step duration, merged LCOV line-coverage percentage, raw `LF` and `LH` totals, E2E spec file count, runner label, and commit SHA.
    - It contains at least one `https://github.com/` GitHub Actions run URL and the matching numeric run id.
    - It contains empty, clearly-labelled "After (sharded matrix)" and "Verdict" sections for plan `01-04`.
    - Every number in the "Before" section is traceable to a run id listed in the "Provenance" section.
    - `.github/workflows/ci.yml` at the moment of the baseline run still had a single job (any artifact-upload step added for coverage retrieval is the only permitted change).
  </acceptance_criteria>
  <done>The pre-change CI wall-clock time and LCOV coverage are committed in BASELINE.md, each attributable to a linked GitHub Actions run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify cypress-split package legitimacy before install</name>
  <files>package.json</files>
  <what-built>The pre-change baseline is committed. Nothing has been installed yet — this gate runs before the second new package is added.</what-built>
  <how-to-verify>
`cypress-split@1.25.0` is required to distribute the 14+ spec files across matrix jobs without a paid Cypress Cloud subscription. Research tagged it `[ASSUMED]` because the package name was discovered via web search rather than Context7 or an official docs page, even though the registry and legitimacy checks both returned OK (actively released, ~436,295 weekly downloads, no postinstall script, source repo github.com/bahmutov/cypress-split). This project's own prior `.planning/research/STACK.md` independently recommended the same package and version, which corroborates but does not upgrade the provenance tier.

Please confirm before the install runs:
  1. Visit https://www.npmjs.com/package/cypress-split
  2. Confirm the repository link points at github.com/bahmutov/cypress-split
  3. Confirm version 1.25.0 exists and the weekly download count is in the hundreds of thousands
  4. Confirm nothing about the listing looks typosquatted or newly republished under a different maintainer
  </how-to-verify>
  <resume-signal>Type "approved" to allow the install, or name a different spec-splitting approach to use instead</resume-signal>
</task>

<task type="tracer">
  <name>Task 3: Run one green CI pass end-to-end through a sharded matrix and a single merged coverage report</name>
  <reversibility rating="costly">Splitting the single `ci` job into five named jobs renames the status checks GitHub reports. Any branch-protection rule that requires the old `Build, Check &amp; Test` check must be re-pointed at the new job names, and that is a repository-settings change outside this repo's files.</reversibility>
  <files>package.json, yarn.lock, cypress.config.ts, .github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the whole current file — this task restructures it)
    - cypress.config.ts (the e2e `setupNodeEvents` body that cypress-split must be registered inside)
    - package.json (the `cypress:e2e`, `test:start:app` and `coverage:merge` scripts)
    - test/utility/TestUtilityDBContainer.ts (confirm the hardcoded 3307/6380 host ports and that no change is needed under a matrix)
    - sonar-project.properties (the single lcov path Sonar consumes)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-PATTERNS.md (`.github/workflows/ci.yml` section — the current E2E step and the target matrix shape, plus the per-shard artifact naming note)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-RESEARCH.md ("System Architecture Diagram — CI matrix + spec sharding" and the Anti-Patterns list)
    - .planning/research/PITFALLS.md (Pitfall 8 — the guard rails this task must not trade away)
  </read_first>
  <behavior>
    - A single push produces five job groups: build-and-lint, unit-tests, component-tests, a two-entry e2e-tests matrix, and merge-coverage-and-sonar.
    - Each e2e matrix job starts its own Testcontainers MySQL and Redis, runs a disjoint subset of the specs, and uploads a uniquely-named coverage artifact.
    - The union of the two shards' spec subsets equals the full spec set — no spec runs twice and none is missed.
    - merge-coverage-and-sonar downloads every coverage artifact, produces one `.test/lcov.info`, and runs SonarQube exactly once.
    - The whole graph is green.
  </behavior>
  <action>
Install the reviewed devDependency with `yarn add -D cypress-split@1.25.0`.

Register it in `cypress.config.ts` inside the `e2e` block's `setupNodeEvents`, calling it with the same `on` and `config` arguments already passed to `codeCoverageTask`, and before the existing `return config` — it narrows `config.specPattern` for the current process based on the `SPLIT` and `SPLIT_INDEX` environment variables. Leave the `TestUtilityDBContainer` wiring, the four `on("task", ...)` handlers, and the `before:run` / `after:run` hooks exactly as they are: each matrix job runs its own Cypress process on its own VM, so those hooks give each shard its own database, which is precisely what D-08 requires. Do not make the container's host ports dynamic — that is unnecessary churn under this architecture, and RESEARCH.md lists it as an anti-pattern.

In the same file, set `video` to false inside the `e2e` block. Video encoding is a large fixed per-spec cost and screenshots-on-failure already cover the debugging need. Leave `retries` untouched at `runMode: 2` — that value and the `after:run` cleanup are non-negotiable guard rails from Pitfall 8, not speed levers.

Restructure `.github/workflows/ci.yml` into five jobs, preserving every step that exists today:

- `build-and-lint` — the current checkout, corepack, setup-node, cache, `yarn install`, `yarn build`, `yarn lint` sequence. Upload `.next/` as a build artifact so downstream jobs do not each repeat the build; keep `yarn db:generate` in the downstream jobs instead, since it writes into `node_modules` and is cheap once dependencies are installed.
- `unit-tests` — needs build-and-lint; runs `yarn test:unit`; uploads its coverage directory as an artifact.
- `component-tests` — needs build-and-lint; runs the component suite as today; uploads its coverage directory as an artifact.
- `e2e-tests` — needs build-and-lint; `strategy` with `fail-fast: false` and a matrix of two shard entries for this first pass. Each job downloads the `.next/` build artifact, runs `yarn db:generate`, then runs the E2E step exactly as the current workflow does (the `cypress-io/github-action@v6` step with the same `start`, `wait-on` and `browser` inputs and the same `NODE_ENV` / `DATABASE_URL` / `SESSION_SECRET_KEY` env vars), adding `SPLIT` sourced from `strategy.job-total` and `SPLIT_INDEX` sourced from `strategy.job-index`. Upload this shard's coverage directory and, on failure, its screenshots — give both artifact names a per-shard suffix from the matrix value so shards do not overwrite each other.
- `merge-coverage-and-sonar` — needs all of unit-tests, component-tests and e2e-tests. Checks out the source (SonarQube needs it), downloads every coverage artifact into `.test/` so each lands in its own subdirectory and the existing `.test/**/*.info` glob picks them all up, runs `yarn coverage:merge`, then runs the two existing SonarQube steps unchanged with their existing `if` conditions.

Scope the repository secrets deliberately: `SONAR_TOKEN` and `SONAR_HOST_URL` belong only on `merge-coverage-and-sonar`. Do not add them to the shard jobs, which have no need for them — the current single-job layout forces every step to sit alongside those secrets, and splitting the graph is an opportunity to narrow that. Do not add any step that prints `DATABASE_URL` to the log.

Guard against an empty shard: with the current spec count a two-way split gives every shard several specs, but add a note in the workflow near the matrix recording that the shard count must stay at or below the spec count, because a shard with no specs fails rather than passing vacuously.

Push and watch one full run. Fix whatever breaks until the entire graph is green, then confirm from the logs that the two shards ran disjoint spec subsets whose union is the full spec set, and that SonarQube ran exactly once against a merged `.test/lcov.info` rather than once per shard.
  </action>
  <verify>
    <automated>grep -Eq 'runMode:[[:space:]]*2' cypress.config.ts &amp;&amp; echo "retries preserved"</automated>
    <automated>grep -c 'cypress-split' cypress.config.ts</automated>
    <automated>grep -c 'matrix' .github/workflows/ci.yml</automated>
    <automated>ls test/cypress/e2e/*.spec.ts | wc -l</automated>
    <automated>yarn lint</automated>
    <automated>gh run list --branch "$(git rev-parse --abbrev-ref HEAD)" --workflow "CI Workflow" --limit 1 --json conclusion --jq '.[0].conclusion'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Eq 'runMode:[[:space:]]*2' cypress.config.ts` succeeds — the retry setting is unchanged.
    - `grep -c 'cypress-split' cypress.config.ts` returns 1 or more, and `package.json` devDependencies contains `cypress-split` at `1.25.0`.
    - `.github/workflows/ci.yml` defines exactly five jobs: build-and-lint, unit-tests, component-tests, e2e-tests, merge-coverage-and-sonar.
    - The e2e-tests job declares `fail-fast: false` and a matrix, and sets `SPLIT` and `SPLIT_INDEX` from `strategy.job-total` and `strategy.job-index`.
    - `ls test/cypress/e2e/*.spec.ts | wc -l` returns the same count as recorded in BASELINE.md's "Before" section — no spec file was removed.
    - `SONAR_TOKEN` and `SONAR_HOST_URL` appear only within the merge-coverage-and-sonar job.
    - The most recent CI run on the branch reports conclusion `success`.
    - The run's logs show each shard executing a disjoint spec subset, the union covering every spec, and exactly one SonarQube step execution.
    - `test/utility/TestUtilityDBContainer.ts` is byte-identical to its phase-start state.
    - The SUMMARY records the run id and URL of the first fully-green sharded run.
  </acceptance_criteria>
  <done>One complete CI run has passed through the new job graph: two isolated E2E shards, disjoint spec subsets covering the whole suite, per-shard coverage artifacts merged once, and a single SonarQube scan.</done>
</task>

</tasks>

<planner_assumptions>
Explicit, surfaced assumptions for this plan — none of these are silently dropped:

1. **Edge-probe row `TEST-01 / unclassified` remains unresolved.** The deterministic edge probe could not classify TEST-01 into any of its eight shape categories and returned `status: unresolved, category: unclassified, probe: "unclassified — review manually"`. Per the probe protocol an unclassified row is never auto-resolved, so it is carried here as a flagged assumption rather than converted into an invented acceptance criterion. What a human should decide: whether the sharding change has a data-shape edge the taxonomy does not reach — the most likely candidate is the boundary where the shard count meets or exceeds the spec count (a shard receives zero or one spec). This plan mitigates the zero-spec case with an explicit workflow note and an at-or-below-spec-count rule, but that mitigation is a planner judgment, not a probe-derived criterion.
2. **UI-SPEC lift is not applicable to this plan.** `01-UI-SPEC.md` states TEST-01 has no UI surface at all. Every non-dismissed row in its "## UI Considerations" section was lifted into plans `01-01` (error and zero-one-many for the counterparty filter) and `01-02` (Interaction Order Contract rows 1 through 6). No UI consideration is dropped by this plan's silence.
3. **Build sharing versus per-shard rebuild is a measured choice.** The plan prescribes sharing `.next/` via an artifact so shards do not each repeat `yarn build`, because repeating an N-minute build N times can erase the sharding win entirely. If the artifact round-trip measures slower than a per-shard rebuild, plan `01-04` may reverse this — record whichever was chosen and why.
4. **Branch protection is outside this repo's files.** Renaming the single `Build, Check &amp; Test` status check into five job-level checks may break a required-status-check rule configured in GitHub repository settings. The executor cannot see or change those settings; if the PR reports a missing required check after the restructure, surface it rather than reshaping the job graph to preserve the old name.
5. **A 15th spec file is expected.** Plan `01-02` adds `formKeyboardNavigation.spec.ts`. The spec count must be re-derived at execution time and recorded in BASELINE.md rather than assumed to be 14.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine and CI | A new devDependency is installed and executes during CI runs |
| GitHub repository secrets → workflow jobs | `SONAR_TOKEN` and `SONAR_HOST_URL` become available to whichever jobs declare them |
| CI job → CI job (artifacts) | Coverage data crosses between jobs as named artifacts before being merged and published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `yarn add -D cypress-split@1.25.0` | high | mitigate | Package legitimacy gate satisfied by the RESEARCH.md audit (registry OK, no postinstall script, real source repo, ~436k weekly downloads, independently corroborated by this project's prior STACK.md research), plus the blocking-human checkpoint immediately before the install task. The package is a devDependency and never ships in the production bundle. |
| T-01-06 | Information Disclosure | Repository secrets across matrix jobs in `.github/workflows/ci.yml` | medium | mitigate | Declare `SONAR_TOKEN` and `SONAR_HOST_URL` only on `merge-coverage-and-sonar`; the shard jobs never receive them. This is strictly narrower than the current single-job layout, where every step sits alongside those secrets. Additionally, no new step may print `DATABASE_URL`, which carries a throwaway test password already committed in plaintext. |
| T-01-07 | Tampering | Per-shard `actions/upload-artifact` names | low | mitigate | Give every shard artifact a unique per-shard suffix from the matrix value. Colliding names would silently overwrite one shard's coverage with another's, producing a merged report that understates coverage and would falsely fail (or falsely pass) the parity check in plan `01-04`. |
| T-01-08 | Tampering | `test/utility/TestUtilityDBContainer.ts` hardcoded host ports 3307 and 6380 | low | accept | Ports collide only when two Cypress processes share a machine. A GitHub Actions matrix places each shard on its own VM with its own Docker daemon, so the collision cannot occur under this architecture. Accepted with the explicit condition that this rating is void if the phase ever switches to in-job sharding. |
</threat_model>

<verification>
- `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md` exists, is non-empty, contains a "Before" section and at least one GitHub Actions run URL.
- `grep -Eq 'runMode:[[:space:]]*2' cypress.config.ts` succeeds.
- `grep -c 'cypress-split' cypress.config.ts` returns 1 or more.
- `.github/workflows/ci.yml` defines the five named jobs with a `fail-fast: false` E2E matrix.
- `ls test/cypress/e2e/*.spec.ts | wc -l` matches the count recorded in BASELINE.md.
- `yarn lint` exits 0.
- The latest CI run on the branch reports conclusion `success`, with disjoint shard spec subsets and exactly one SonarQube execution.
- `test/utility/TestUtilityDBContainer.ts` is byte-identical to its phase-start state.
</verification>

<success_criteria>
The pre-change baseline required by phase success criteria 3 and 4 is committed and attributable to a real CI run, and the sharded matrix architecture is proven green end-to-end — every spec still runs, database isolation is per-shard, coverage merges once, and SonarQube scans once.
</success_criteria>

<artifacts_this_phase_produces>
Symbols this plan creates (new — exclude from drift verification against pre-existing code):

- New file `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md`, with sections "Before (sequential, single job)", "After (sharded matrix)", "Verdict", and "Provenance".
- New devDependency in `package.json`: `cypress-split@1.25.0` (and its `yarn.lock` entries).
- New `cypress-split` import and registration call inside `cypress.config.ts`'s `e2e.setupNodeEvents`, plus a new `video: false` key in the `e2e` block.
- New CI job names in `.github/workflows/ci.yml`: `build-and-lint`, `unit-tests`, `component-tests`, `e2e-tests`, `merge-coverage-and-sonar` (replacing the single `ci` job named `Build, Check &amp; Test`).
- New CI artifact names: a `.next/` build artifact, per-job coverage artifacts for unit and component, and per-shard coverage and screenshot artifacts suffixed by the matrix value.
- New workflow environment variables `SPLIT` and `SPLIT_INDEX` on the E2E shard step.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-bug-fixes-e2e-baseline/01-03-SUMMARY.md` when done.
</output>
