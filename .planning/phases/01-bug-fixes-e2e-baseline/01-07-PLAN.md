---
phase: 01-bug-fixes-e2e-baseline
plan: 07
type: execute
wave: 2
depends_on: ["01-05", "01-06"]
files_modified:
  - .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [TEST-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LCOV coverage after the sharded-matrix speed-up is equal to or higher than the pre-change baseline (ROADMAP Phase 1 Success Criterion 4; REQUIREMENTS.md TEST-01: 'coverage no worse than the current baseline') (01-VERIFICATION.md failed truth #14, restated verbatim — this plan decides it on fresh evidence)"
    - "The deciding coverage figure is read from a named, linked GitHub Actions run that includes both gap-closure plans' changes — not recomputed locally, estimated, or carried over from an earlier run (D-11)"
    - "Merged LH/LF from that run is compared against the Before baseline of LH 2956 / LF 3827 on raw totals, and the comparison is decided strictly: equal or higher passes, one line lower fails"
    - "If the figure is still below the baseline, a human — not the executing agent — decides whether to accept the shortfall, and that decision is recorded verbatim in BASELINE.md with who made it and when"
    - "REQUIREMENTS.md's TEST-01 row no longer reads 'Pending': it records either the passing run-sourced figures, or an explicitly accepted shortfall naming the recorded decision"
    - "The counterparty-filter regression spec and formKeyboardNavigation spec both pass in the same run that produces the coverage figure, and the new shard-count guard step passes in all four shards"
    - "Every CI attempt made during this plan is recorded, including failures and job reruns — no failed attempt is discarded to manufacture a clean result"
  prohibitions:
    - statement: "MUST NOT record any figure in BASELINE.md or REQUIREMENTS.md that was not read from an actual, linkable GitHub Actions run — no locally-timed, estimated, recalled, or carried-over numbers"
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT mark TEST-01 complete on the executing agent's own judgement when the measured figure is below the baseline — a shortfall requires a recorded human decision, and auto-mode must not supply it"
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT rerun the workflow until a favourable coverage number appears and then report only that run — the first fully-green run at HEAD is the figure, and every attempt is disclosed"
      category: transparency
      status: resolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md"
      provides: "Rewritten criterion-4 verdict decided on a post-gap-closure CI run, with the full attempt log and any recorded human override"
      contains: "Verdict"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TEST-01 traceability row reflecting the decided outcome instead of 'Pending'"
      contains: "TEST-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md"
      via: "the TEST-01 row's status points at the run-sourced figures and, if applicable, the recorded override in BASELINE.md"
      pattern: "TEST-01"
    - from: ".planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md"
      to: "GitHub Actions merged-lcov artifact"
      via: "the deciding LH/LF figure names the run ID and artifact it was read from"
      pattern: "https://github.com/"
---

<objective>
Decide 01-VERIFICATION.md's failed truth #14 on fresh evidence: run CI with both gap-closure plans' changes in place, read the merged coverage from that named run, and either close TEST-01 on the numbers or put an accurately-stated shortfall in front of a human for an explicit accept-or-not decision.

Purpose: The coverage criterion has been open since plan `01-04` closed with an honest "MET WITH A SMALL, DIAGNOSED SHORTFALL" verdict, which is not the same as met. Plan `01-06` attempts the fix; this plan produces the CI-sourced number that decides it and — critically — refuses to settle a `must_haves` gate failure by agent judgement when the number still falls short.
Output: A rewritten criterion-4 verdict in BASELINE.md backed by a linked run, a REQUIREMENTS.md TEST-01 row that no longer reads "Pending", and, where needed, a recorded human decision rather than a quiet acceptance.

**MVP-mode note:** the ROADMAP `**Goal:**` line for this phase is an outcome sentence, not `As a / I want to / so that` user-story form; no story was invented here. This plan carries no tracer task: it does not build a new path, it measures and decides on the path plans `01-05` and `01-06` already completed end to end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-bug-fixes-e2e-baseline/01-CONTEXT.md
@.planning/phases/01-bug-fixes-e2e-baseline/01-VERIFICATION.md
@.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md
</context>

<interface_context>
Facts the executor needs without re-deriving them:

- **The bar.** Before: LH `2956` / LF `3827` = 77.24% (run `32869899958`). The criterion is a strict `>=` on the raw totals: LH must be at least `2956` with LF still `3827`. A rounded percentage must not be used to decide it.
- **How CI is triggered here.** `.github/workflows/ci.yml` runs on push to `main` and on `pull_request` targeting any branch. Plans `01-03` and `01-04` triggered every measurement run by pushing a branch and opening a pull request purely as a CI vehicle (PRs #80, #82, #83 — all closed afterwards, branches deleted; see BASELINE.md's Provenance section). This plan follows the same pattern, except the branch carries real work that is meant to land, so the pull request should not be discarded.
- **Where the number comes from.** The `merge-coverage-and-sonar` job uploads the merged file as the artifact named `merged-lcov` (path `.test/lcov.info`), added permanently in plan `01-04`. Download it with `gh run download <run-id> --name merged-lcov`.
- **How to compute LF and LH.** The merged file carries no `LF:`/`LH:` summary records — count `DA:<line>,<hits>` records for LF, and those with a hit count greater than zero for LH. This is the same method plans `01-04` and `01-06` used, so the figures are directly comparable.
- **Which repository owns the runs.** BASELINE.md's Provenance links point at `github.com/financer-project/financer` while the local `origin` remote is `github.com/raphaelmue/financer-blitz`. Resolve which one actually serves the new run before recording its URL, and keep the recorded URL consistent with where the run really lives.
- **Known suite flakiness.** BASELINE.md records that 4 of 7 attempts at the adopted 4-shard count failed on first try with distinct, non-repeating, pre-existing spec-level flakes (`mobile`, `accounts`, `counterparties`, `tags`), none traceable to the shard-distribution logic. Expect this. It is disclosed, not fixed here — `retries.runMode` stays at 2 per PITFALLS #8.
- **What plans 01-05 and 01-06 changed that this run must validate.** `test/cypress/e2e/transactions.spec.ts` now asserts no uncaught exception occurred; `test/cypress/support/e2e.ts` types `realPress` against the library key union; `.github/workflows/ci.yml` gained a "Guard shard count against spec-file count" step in each shard; new or extended Vitest tests exist under `test/vitest/`.
- **The already-known non-cause.** Component-test coverage has never been collected (`deferred-items.md`) — identically in Before and After. Do not use it to explain or close a residual gap.
- **`config.json` settings that bind here.** `workflow.human_verify_mode` is `end-of-phase`, so no mid-flight human-verify checkpoint is emitted. `mode` is `yolo` and `workflow.auto_advance` is `false`; the decision checkpoint in Task 2 carries `gate="blocking-human"` specifically so no auto-mode path can answer it on the developer's behalf.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Run CI with both gap-closure plans in place and read the merged coverage from that run</name>
  <precondition>`gh auth status` succeeds with push access to the `origin` remote, and the working tree is clean with plans `01-05` and `01-06` already committed on the current branch (`git status --porcelain` is empty and `git log --oneline -5` shows both plans' commits).</precondition>
  <files>.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</files>
  <read_first>
    - .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md ("Coverage comparison (D-10, criterion 4)", the "Coverage gap-closure investigation (plan 01-06)" section written last wave, the flake-check table's format, and the Provenance section to extend)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-05-SUMMARY.md (what changed in the spec, support file and workflow, and the local red/green evidence)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-06-SUMMARY.md (which lines were closed, which were not, and the local LF/LH figures to compare the CI figure against)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-CONTEXT.md (D-11 — every figure must come from an actual CI run)
    - .github/workflows/ci.yml (confirm the merged-lcov upload step and the new shard guard step are both present on the branch being pushed)
  </read_first>
  <behavior>
    - A CI run executes at the current HEAD, covering both gap-closure plans' changes.
    - All four E2E shards report the guard step passing with its spec and shard counts visible in the log.
    - `transactions.spec.ts` and `formKeyboardNavigation.spec.ts` both pass in that run.
    - The merged LCOV artifact from the first fully-green run is downloaded and its LF and LH computed.
    - Every attempt — including failures and any job rerun — is recorded with its run ID, conclusion and failing spec.
  </behavior>
  <action>
Push the current branch to `origin` and ensure an open pull request targeting `main` exists for it, creating one with `gh pr create` if none exists and reusing it if one does. Do not merge it, and do not open a throwaway branch — unlike plans `01-03` and `01-04`, the commits on this branch are real work meant to land. Confirm the pushed HEAD contains both `01-05`'s and `01-06`'s commits before waiting on the run.

Watch the run with `gh run watch` or poll `gh run list --branch "$(git rev-parse --abbrev-ref HEAD)" --workflow "CI Workflow"`. When it finishes, record its ID, URL and conclusion regardless of outcome.

If it fails, diagnose before rerunning. A failure inside `transactions.spec.ts` or `formKeyboardNavigation.spec.ts` is this phase's own work and must be fixed, not retried — those two specs were changed last wave. A failure in the shard guard step is a defect in `01-05` Task 3 and must be fixed. A failure in any of `mobile`, `accounts`, `counterparties` or `tags` matches the pre-existing flake pattern BASELINE.md already documents; rerun that job with `gh run rerun --failed` and disclose the rerun rather than presenting the result as a clean first-try green. Record every attempt in the flake-check table's existing format — run ID, conclusion, and the specific failing spec and assertion.

From the first fully-green run, download the merged coverage with `gh run download <run-id> --name merged-lcov` and compute LF and LH by counting `DA:` records and those with a hit count greater than zero. Do not recompute the figure locally from a local test run; D-11 requires it come from the CI artifact. Delete or keep the downloaded artifact outside the working tree so it is not committed.

Also confirm from the run's logs, and record: that the guard step printed a spec count and a shard count in each of the four shards and exited zero; that the four shards' spec subsets are still disjoint with a union of the full spec set; and that the counterparty and keyboard-navigation specs both appear as passed.

Append an "After (post-gap-closure)" subsection to BASELINE.md's coverage material recording the run ID, the run URL, LF, LH, the derived percentage, and the delta against the Before baseline of LH `2956` / LF `3827`. Add the attempt log. Extend Provenance with every new run URL. Do not rewrite the "Verdict" section in this task — Task 3 does that once the outcome, including any human decision, is settled.

Note the branch state plainly in the SUMMARY: which branch was pushed, which pull request number was used, and that it was left open rather than merged or closed.
  </action>
  <verify>
    <automated>git status --porcelain</automated>
    <automated>gh run list --branch "$(git rev-parse --abbrev-ref HEAD)" --workflow "CI Workflow" --limit 1 --json databaseId,conclusion,url --jq '.[0]'</automated>
    <automated>grep -c 'After (post-gap-closure)' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
    <automated>grep -c 'https://github.com/' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
    <automated>git diff --name-only HEAD | sort</automated>
  </verify>
  <acceptance_criteria>
    - A CI run at the current HEAD reports conclusion `success`, and its ID and URL are recorded in BASELINE.md.
    - BASELINE.md contains an "After (post-gap-closure)" subsection with the run ID, run URL, LF, LH, derived percentage, and the raw delta against LH 2956 / LF 3827.
    - The recorded LF is `3827`; if it is not, that is reported as a like-for-like break and diagnosed rather than presented as a coverage result.
    - The attempt log lists every run triggered by this plan with its conclusion, and names the failing spec and assertion for any failure.
    - BASELINE.md records that the shard guard step passed in all four shards with its printed counts, that the shard spec subsets were disjoint and complete, and that `transactions.spec.ts` and `formKeyboardNavigation.spec.ts` both passed.
    - `git status --porcelain` is empty and no downloaded artifact appears in `git diff --name-only HEAD`.
    - The SUMMARY names the branch pushed and the pull request number, and states it was left open.
  </acceptance_criteria>
  <done>A single named, linked, green CI run at HEAD has produced the coverage figure that decides criterion 4, with every attempt disclosed.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Decide what to do about a residual coverage shortfall (CONDITIONAL — only if LH is still below 2956)</name>
  <files>.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</files>
  <read_first>
    - The "After (post-gap-closure)" subsection Task 1 wrote into BASELINE.md (the deciding figures)
    - .planning/phases/01-bug-fixes-e2e-baseline/01-06-SUMMARY.md (which lines were closed, which were not, and the individual reason recorded for each remaining line)
    - .planning/ROADMAP.md (Phase 1 Success Criterion 4 — the exact wording being decided against)
    - .planning/REQUIREMENTS.md (TEST-01's wording and its current 'Pending' status)
  </read_first>
  <decision>
Merged LCOV line coverage at HEAD is still below the pre-change baseline of LH 2956 / LF 3827 (77.24%) after the gap-closure attempt. ROADMAP Phase 1 Success Criterion 4 and REQUIREMENTS.md TEST-01 both require coverage no worse than that baseline. How should this be resolved?

**If Task 1 measured LH at 2956 or higher, this checkpoint does not apply.** Skip it, record in the SUMMARY that it was skipped because the criterion was met on the numbers, and proceed directly to Task 3.
  </decision>
  <context>
This is a `must_haves` gate failure carried over from 01-VERIFICATION.md. The executing agent is explicitly not permitted to settle it by its own judgement — that is what left the criterion in an ambiguous "met with a shortfall" state after plan `01-04` and is why the phase verification found a gap.

Present the developer with: the exact measured LH and LF and the run URL; the size of the residual gap in lines and percentage points; the list of lines plan `01-06` could not close, each with the individual reason recorded for it; and the fact that LF is unchanged, so this is not a measurement artefact or a dropped-file defect (that class of defect was already found and fixed in plan `01-04`).
  </context>
  <options>
    <option id="accept-shortfall">
      <name>Accept the residual shortfall and record an explicit override</name>
      <pros>Unblocks phase close now. The gap is a fraction of a percentage point on a stable, reproducible measurement, LF is unchanged so nothing was dropped, and every remaining line carries a written reason. The speed-up it bought is decisive: 16m25s to 11m24s total, 10m27s to 6m46s on the E2E stage.</pros>
      <cons>A roadmap success criterion is closed by decision rather than by measurement. The precedent makes the next re-baselining conversation harder, and any future coverage drift starts from an accepted-shortfall reference rather than a clean one.</cons>
    </option>
    <option id="keep-closing">
      <name>Keep closing — another coverage pass before phase close</name>
      <pros>Meets the criterion literally with no override on the record. The remaining lines are enumerated and classified, so a further pass is targeted rather than exploratory.</pros>
      <cons>Costs another plan and another CI cycle. If the remaining lines are genuinely order-sensitive or fault-only, the pass may end in the same place, and coverage added elsewhere purely to make the total clear the bar is closer to gaming the number than to closing the gap.</cons>
    </option>
    <option id="revert-sharding">
      <name>Revert the sharded matrix and restore exact parity</name>
      <pros>Restores the Before coverage figure by construction, since the drop is attributable to parallel-versus-sequential execution order.</pros>
      <cons>Abandons TEST-01's entire purpose and Success Criterion 3, which is decisively met. Trades a proven five-minute CI saving for roughly ten lines of order-dependent coverage. Listed for completeness; it is the worst trade of the three.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The checkpoint is presented only when Task 1 measured LH below 2956; otherwise it is skipped and the skip is recorded in the SUMMARY with the measured figures.
    - The presentation includes the measured LH and LF, the run URL, the gap in lines and percentage points, and the per-line reasons from plan `01-06`.
    - The developer's chosen option and any accompanying wording is recorded verbatim, along with the date, for Task 3 to transcribe into BASELINE.md.
    - The choice is made by a human. This checkpoint carries `gate="blocking-human"` and must not be auto-approved or auto-selected under any mode.
  </acceptance_criteria>
  <resume-signal>Select: accept-shortfall, keep-closing, or revert-sharding — and add any wording you want recorded verbatim in BASELINE.md alongside the decision.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Rewrite the criterion-4 verdict and update TEST-01's status</name>
  <reversibility rating="costly">Recording an accepted coverage shortfall against a roadmap success criterion becomes the reference point for every later re-baselining; undoing it means reopening the requirement and redoing the investigation.</reversibility>
  <files>.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md (the existing "Verdict" section's criterion 4 paragraph to replace, and the "After (post-gap-closure)" figures from Task 1)
    - The decision recorded by Task 2, if it ran
    - .planning/REQUIREMENTS.md (the TEST-01 bullet in "Test Infrastructure" and the TEST-01 row in the Traceability table — both currently unchecked / 'Pending')
    - .planning/phases/01-bug-fixes-e2e-baseline/01-VERIFICATION.md (gap 2's `missing` item — the two acceptable closures, so the recorded outcome matches one of them exactly)
    - .planning/ROADMAP.md (Phase 1 Success Criterion 4's exact wording)
  </read_first>
  <behavior>
    - BASELINE.md's Verdict section carries a current criterion-4 verdict stating plainly whether the criterion is met, on the post-gap-closure figures.
    - The plan `01-04` criterion-4 paragraph survives as labelled history rather than being deleted, so the correction is visibly a correction.
    - If a human decision was taken, it appears verbatim with the date and the option chosen.
    - REQUIREMENTS.md's TEST-01 entries reflect the same outcome and point at the evidence.
    - No figure appears in either file that is not traceable to a named run.
  </behavior>
  <action>
Update BASELINE.md's "Verdict" section for criterion 4. Do not delete plan `01-04`'s existing criterion-4 paragraph: prefix it with an explicit `Superseded by plan 01-07` label so it survives as dated history, then write the current verdict immediately below it. Erasing the earlier verdict would hide that a correction happened, which is the opposite of what this document is for.

The current verdict states the post-gap-closure LH and LF, the derived percentage, the run ID and URL they came from, and a direct outcome: met, or not met and accepted by explicit decision, or not met and open. Keep the historical Before / 2-shard / 4-shard figures from plans `01-03` and `01-04` intact. Add a one-line pointer from the verdict to the "Coverage gap-closure investigation (plan 01-06)" section so a reader can follow the line-level evidence.

If Task 2 ran, add a subsection recording the decision: the option chosen, the developer's verbatim wording, the date, and the figures that were in front of them when they chose. Frame it as a decision, not as a measurement — a reader must be able to tell at a glance which parts of this document are measured and which are agreed.

Then update `.planning/REQUIREMENTS.md` with a scoped edit, never a whole-file rewrite. Change the TEST-01 bullet under "Test Infrastructure" and the TEST-01 row in the Traceability table to reflect the decided outcome. Where the criterion was met on the numbers, record the deciding LH/LF and the run ID alongside the status. Where it was accepted by decision, say so explicitly in the status rather than writing a bare "Complete" — a future reader must not mistake an agreed close for a measured one. Leave the BUG-01 and BUG-02 rows alone: their re-verification belongs to `/gsd-verify-work`, not to this plan.

Do not modify ROADMAP.md, and do not edit any other requirement row. Do not change any figure recorded by an earlier plan.
  </action>
  <verify>
    <automated>grep -c 'Verdict' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
    <automated>grep -n 'TEST-01' .planning/REQUIREMENTS.md</automated>
    <automated>grep -c 'Superseded by plan 01-07' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
    <automated>grep -c 'https://github.com/' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md</automated>
    <automated>git diff --name-only HEAD | sort</automated>
  </verify>
  <acceptance_criteria>
    - BASELINE.md's "Verdict" section states the criterion-4 outcome using the post-gap-closure figures and names the run ID and URL they came from.
    - `grep -c 'Superseded by plan 01-07' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md` returns at least 1 — plan `01-04`'s criterion-4 paragraph is labelled as history rather than deleted.
    - The current criterion-4 verdict appears below that labelled paragraph and is unambiguous: met, not met and accepted by recorded decision, or not met and open.
    - The historical Before / 2-shard / 4-shard figures recorded by plans `01-03` and `01-04` are unchanged.
    - If Task 2 ran, BASELINE.md contains a decision subsection with the chosen option, the developer's verbatim wording, and the date.
    - `grep -n 'TEST-01' .planning/REQUIREMENTS.md` shows both the bullet and the traceability row updated away from 'Pending', with the status distinguishing "met on the numbers" from "accepted by recorded decision".
    - The BUG-01 and BUG-02 rows in REQUIREMENTS.md are unchanged by this task.
    - `git diff --name-only HEAD` lists only `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md` and `.planning/REQUIREMENTS.md`.
    - `.planning/ROADMAP.md` is unchanged.
  </acceptance_criteria>
  <done>TEST-01's status is decided and recorded, with measured facts and agreed judgements visibly separated, so the next verification pass can match the outcome against 01-VERIFICATION.md's gap 2 one-to-one.</done>
</task>

</tasks>

<planner_assumptions>
Explicit, surfaced assumptions and probe dispositions for this plan — none silently dropped:

1. **Edge-probe `TEST-01 / unclassified` — RESOLVED (explicit) at the decision boundary.** The boundary this plan sits on is the `>=` comparison itself. It is applied strictly on raw LH and LF totals; "equal to" passes, "one line lower" fails and routes to Task 2. The other TEST-01 edge (shard count versus spec count) is owned by plan `01-05` Task 3, and the coverage-parity forensics by plan `01-06`.
2. **Prohibition-probe — three bespoke items minted, canon breadcrumbed.** Stage 2 kept the measurement-integrity family: no unsourced figures, no agent-settled shortfall, no rerun-until-favourable. CI secret handling and workflow-permission concerns are canon owned by `/gsd-secure-phase` and are breadcrumbed, not minted. The `.claude/CLAUDE.md` household-isolation and layered-architecture constraints were surfaced at Stage 1 and dropped at Stage 2 — this plan touches no `src/` code and no data path.
3. **The checkpoint is conditional by design.** Presenting a decision when the numbers already pass would be checkpoint fatigue for no gain. Presenting nothing when they fail would be the exact failure 01-VERIFICATION.md flagged. Task 2 therefore runs only on the failing branch, and its skip must be recorded rather than silent.
4. **`gate="blocking-human"` is load-bearing.** `config.json` sets `mode: yolo`; a plain `blocking` gate would let auto-mode select the first option, which is `accept-shortfall`. Auto-accepting a requirement shortfall is precisely what the gap input forbids, so the stronger gate is used.
5. **One green run is the bar, not three.** Success Criterion 5's three-consecutive-green requirement was already satisfied and recorded by plan `01-04`; re-proving it is scope creep. This plan needs one green run at HEAD to source the coverage figure, with every attempt disclosed. If the developer wants the stability evidence refreshed after these changes, that is a separate call.
6. **Pushing a branch and opening a pull request is a deliberate, precedented action.** Plans `01-03` and `01-04` did exactly this (PRs #80, #82, #83, all recorded in BASELINE.md's Provenance). The difference here is that the branch carries work meant to land, so the pull request is left open rather than closed and deleted. Nothing is merged by this plan.
7. **Repository ambiguity is unresolved and must be settled at execution time.** BASELINE.md's run URLs point at `financer-project/financer` while `origin` is `raphaelmue/financer-blitz`. The executor must determine which serves the new run before recording a URL, and note the answer in the SUMMARY.
8. **A not-met verdict is a valid completion of this plan.** If the developer chooses `keep-closing`, this plan completes by recording that accurately and leaving TEST-01 open. Manufacturing a pass is worse than an honest open item.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI run output → committed project documentation | The figure that decides a roadmap success criterion crosses from an external system into durable artifacts the verifier and future phases rely on |
| Agent judgement → recorded requirement status | A requirement can be marked complete by writing text; the gate that stops an agent doing so on a failing number is procedural, not technical |
| Local repository → remote repository | This plan pushes commits and opens a pull request on the developer's repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-21 | Spoofing | TEST-01 status in `.planning/REQUIREMENTS.md` | high | mitigate | A "Complete" written by the agent on a failing measurement is indistinguishable from one earned. Task 2's `gate="blocking-human"` checkpoint is the control, and Task 3 must record an agreed close in wording that distinguishes it from a measured one. |
| T-01-22 | Repudiation | Figures in BASELINE.md's rewritten verdict | medium | mitigate | Every figure names the run ID and URL it was read from; the historical figures from plans `01-03` and `01-04` are asserted unchanged so the correction is additive rather than an erasure. |
| T-01-23 | Tampering | CI attempt selection | medium | mitigate | The figure comes from the first fully-green run at HEAD, and every attempt including failures and job reruns is disclosed in the attempt log — rerunning until a favourable number appears is explicitly prohibited. |
| T-01-24 | Elevation of Privilege | Pushing to `origin` and opening a pull request | low | mitigate | Scoped to pushing the current working branch and opening or reusing a pull request against `main`. Nothing is merged, no branch is force-pushed, no protected ref is written, and the branch and PR number are named in the SUMMARY. |
| T-01-25 | Information Disclosure | `gh run download` / workflow log inspection | low | accept | Workflow logs contain only the throwaway test `DATABASE_URL` already committed in plaintext in `ci.yml`; no production credentials are in scope, and only durations, coverage totals and run URLs are copied into BASELINE.md. Canon note: CI secret handling is owned by `/gsd-secure-phase`; not minted as a project prohibition here. |

No package-manager install occurs in this plan, so no `T-01-SC` supply-chain row applies. No schema-relevant file (Prisma schema, migration) is touched, so no schema-push task is present. No `src/` code changes, so no new runtime attack surface is introduced.
</threat_model>

<verification>
- A green CI run at HEAD exists, and its ID and URL appear in BASELINE.md's "After (post-gap-closure)" subsection alongside LF, LH and the derived percentage.
- The recorded LF is 3827; the recorded LH is compared against 2956 on raw totals, not on a rounded percentage.
- BASELINE.md's attempt log lists every run this plan triggered, with conclusions and any job reruns disclosed.
- BASELINE.md records that the shard-count guard passed in all four shards and that `transactions.spec.ts` and `formKeyboardNavigation.spec.ts` both passed in the deciding run.
- `grep -c 'Superseded by plan 01-07' .planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md` returns at least 1, and a current criterion-4 verdict appears below the labelled historical paragraph.
- If the measured LH was below 2956, BASELINE.md contains a decision subsection with the option chosen, the developer's verbatim wording and the date; if it was at or above 2956, the SUMMARY records the checkpoint as skipped with the figures that made it unnecessary.
- `.planning/REQUIREMENTS.md`'s TEST-01 bullet and traceability row no longer read 'Pending', and their status distinguishes a measured close from an agreed one.
- `.planning/ROADMAP.md` and the BUG-01 / BUG-02 requirement rows are unchanged by this plan.
</verification>

<success_criteria>
01-VERIFICATION.md's failed truth #14 is closed by exactly one of the two closures that gap named: either merged LH/LF from a named post-gap-closure CI run is at or above the Before baseline of LH 2956 / LF 3827, or a human has explicitly decided to accept the shortfall and that decision is recorded verbatim with its date and the figures it was made on. Either way, TEST-01 no longer sits at 'Pending' with no recorded resolution, every figure is traceable to a linked run, and a reader can tell at a glance which parts of the verdict were measured and which were agreed.
</success_criteria>

<artifacts_this_phase_produces>
Symbols this plan creates or modifies (new/changed — exclude from drift verification against pre-existing code):

- New "After (post-gap-closure)" subsection in `.planning/phases/01-bug-fixes-e2e-baseline/BASELINE.md` carrying the deciding run ID, run URL, LF, LH, derived percentage and delta against the Before baseline.
- New attempt-log rows appended to BASELINE.md's flake-check table for every run this plan triggered.
- New current "Criterion 4 (coverage parity)" verdict paragraph in BASELINE.md's "Verdict" section, plus a `Superseded by plan 01-07` label prefixed onto plan `01-04`'s original criterion-4 paragraph, which is retained as dated history.
- Conditional new decision subsection in BASELINE.md recording the developer's chosen option, verbatim wording and date, present only when the checkpoint ran.
- Extended "Provenance" entries in BASELINE.md for every run URL introduced by this plan.
- Updated TEST-01 bullet under "Test Infrastructure" and updated TEST-01 row in the Traceability table of `.planning/REQUIREMENTS.md`.
- A pull request on `origin` used as the CI vehicle, left open — its number recorded in the SUMMARY.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-bug-fixes-e2e-baseline/01-07-SUMMARY.md` when done.
</output>
